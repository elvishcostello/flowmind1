// Canned responses for demo mode.
// Each export must match the exact JSON shape returned by its corresponding API route.

export const intakeResponse = {};

export const treatmentResponse = {};

export const summaryResponse = {};
